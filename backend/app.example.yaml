server:
  port: 8080

data:
  dav: data/dav

db:
  driver-name: mysql
  url: [user]:[password]@tcp([host]:port)/ndisk?charset=utf8mb4&parseTime=True&loc=Local

log:
#  file: app.log # if removed,log in console
  level: info # info,debug,error

cache:
  type: redis # redis,memory
  redis:
    url: redis://<user>:<pass>@localhost:6379/<db>
  expire: 60 # seconds


cloud189:
  cookie: ''